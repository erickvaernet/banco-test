import React from 'react'
import './Table.css';

const Table = (props) => {
    const {}=props;
  return (
    <Table className="table-primary">
        map({props})
    </Table>
  )
}

export default Table;